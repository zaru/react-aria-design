import { defineConfig } from "vite";

/**
 * Storybook（@storybook/react-vite）がこの設定をマージします。
 * Vite 8 ではビルドに Rolldown・トランスフォームに Oxc が使われます。
 * @see https://vitejs.dev/guide/migration.html
 */
export default defineConfig({});
